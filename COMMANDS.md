# OpenHanako Slash Commands

本文档列出 OpenHanako 社交平台机器人支持的所有斜杠指令。

## 会话管理命令

### `/stop` (别名: `/abort`, `/halt`)
- **权限**: owner
- **功能**: 中止当前正在进行的回复
- **说明**: 如果当前没有活动回复，会返回提示信息

### `/new`
- **权限**: owner
- **功能**: 开启新会话，旧会话历史归档
- **说明**: 
  - 如果之前无历史记录，会提示"已开启新会话（之前无历史记录）"
  - 接管桌面会话期间禁止使用

### `/reset` (别名: `/clear`)
- **权限**: owner
- **功能**: 彻底重置会话，清除所有历史
- **说明**: 
  - 破坏性操作，会删除会话文件
  - 接管桌面会话期间禁止使用

### `/clean`
- **权限**: owner
- **功能**: 清除当前对话者的聊天历史，避免上下文污染
- **说明**: 
  - 群聊场景：只清除发送该指令的用户的消息
  - 私聊场景：等同于 /reset，清除整个会话
  - 会删除文件并移除索引引用，确保下次消息创建全新 session
  - 接管桌面会话期间禁止使用

## 上下文压缩命令

### `/compact`
- **权限**: owner
- **功能**: 压缩当前会话上下文
- **说明**: 
  - 会发送"正在压缩..."和"压缩完成/失败"两条消息
  - 显示压缩前后的 token 数量变化
  - 接管态下压缩目标是桌面 session 的上下文

### `/fresh-compact` (别名: `/freshcompact`)
- **权限**: owner
- **功能**: 使用最新提示词和记忆状态压缩当前会话上下文
- **说明**: 
  - 刷新提示词与记忆状态后再压缩
  - 显示压缩前后的 token 数量变化
  - 接管桌面会话期间禁止使用

## 桌面会话接管命令

### `/rc`
- **权限**: owner
- **功能**: 接管桌面会话（远程遥控）
- **说明**: 
  - 只能在 bridge 私聊中使用，群聊不可用
  - 列出当前 agent 最近 10 个桌面会话供选择
  - 回复编号进行接管，5 分钟内不选自动取消
  - 已接管状态下需先 /exitrc 再切换

### `/exitrc`
- **权限**: owner
- **功能**: 退出桌面会话接管
- **说明**: 
  - 只能在 bridge 私聊中使用
  - 未处于接管状态时会提示

## 权限说明

- **owner**: 只有平台账号的 owner 可以执行
- **guest**: 群聊中的非 owner 成员不能使用这些命令（guest 输入的斜杠会作为普通文本发送给 AI）

## 注意事项

1. 所有命令都只能在社交平台机器人的私聊或群聊中使用
2. 接管桌面会话期间，部分命令（/new, /reset, /clean, /fresh-compact）会被禁止使用
3. 命令执行失败时，机器人会返回相应的错误提示
4. 上下文压缩命令会显示压缩前后的 token 数量变化
